import SConsider, pdb

Import('*')

env = SConsider.cloneBaseEnv()
conf = SConsider.Configure(env)
if conf.CheckLibWithHeader('z', 'zlib.h', 'C') and conf.CheckCHeader('gzio.h'):
    conf.env.Append(CPPDEFINES='HAS_ZLIB')
env = conf.Finish()

buildSettings = {}
if 'HAS_ZLIB' in env['CPPDEFINES']:
    buildSettings[packagename] = {    
        'public' : {
            'appendUnique'     : {
                'LIBS'         : ['z'],
            },
        },
    }

SConsider.createTargets(packagename, buildSettings)
